# SDK Stubs

**Note:** These files are local development stubs for working with the full openclaw repository.

When building as a published plugin, use the official plugin SDK via `../runtime-api.js` instead.
